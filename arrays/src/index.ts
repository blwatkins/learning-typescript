// Array Notes

/* NOTES:
** - In JavaScript, each element can be a different type
*/

let numbers: number[] = [23, 83, 28];
console.log(numbers);

let names: string[] = ['Bill', 'Joe', 'Catherine'];
console.log(names);
